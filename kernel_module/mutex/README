Mutex module example
====================

1. compile the mutex module
$ make

2. Install the mutex module
$ sudo insmod mutex.ko

3. Observe the kernel message
$ sudo dmesg

4. Remove the mutex module
$ sudo rmmod mutex
